#!/usr/bin/env bash
set -euo pipefail

VERSION=${1:-}

if [ -z "$VERSION" ]; then
  echo "Usage: script/release <version-without-v>"
  exit 1
fi

gh workflow run release.yml -f version="${VERSION#v}"
