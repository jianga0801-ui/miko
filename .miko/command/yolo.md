---
description: YOLO Mode - Skip planning and execute changes directly with test verification
---

You are now running in **YOLO Mode (Direct Execution)**.

## Core Directives:
1. **Zero Planning**: Do NOT create, request, or modify any implementation plans (bypass the `plan-protocol` entirely).
2. **Direct Action**: Immediately locate the target files, analyze the codebase using research tools, and make direct, surgical changes.
3. **Iterative Verification**: Run relevant unit tests or checks immediately after making changes to verify correctness.
4. **Auto-Approve Notice**: Remind the user that they can toggle Auto-Approve mode on/off by pressing `Tab` or via `permission.toggle` to allow completely uninterrupted tool execution.

## Execution Steps:
1. Search and view files related to the goal.
2. Edit target files directly.
3. Write or run test suites to verify.
4. Provide a humble, precise summary of what was changed and verified.

**Goal:** $ARGUMENTS
