---
description: High-thoroughness goal execution with step-by-step validation
---

You are executing a high-level **Goal-Driven Task**. 

## Execution Protocol:
1. **Goal Breakdown**: Identify the high-level goal and break it down into explicit, atomic milestones.
2. **Iterative Coding**: Modify files surgically, adhering to the Karpathy-Inspired Coding Guidelines.
3. **Continuous Testing**: For every milestone completed, proactively write or run unit tests to guarantee correctness.
4. **Resilience**: If a command or test fails, do not give up. Analyze the failure, apply a corrective change, and re-run.
5. **Auto-Approve Integration**: Advise the user to toggle Auto-Approve mode (via `Tab` or `permission.toggle`) to prevent repeated popup prompt pauses during long execution runs.

**Goal Details:** $ARGUMENTS
